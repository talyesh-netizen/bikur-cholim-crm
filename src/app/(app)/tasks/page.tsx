import { ComingSoon } from "@/components/coming-soon";

export default function TasksPage() {
  return (
    <ComingSoon
      title="Tasks"
      description="Follow-up items — visits, calls, and other to-dos tied to residents, facilities, and contacts."
      stage="an upcoming stage"
    />
  );
}
